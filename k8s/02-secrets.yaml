apiVersion: v1
kind: Secret
metadata:
  name: htg-crm-secrets
  namespace: htg-crm-prod
  annotations:
    note: "Placeholder only. Replace with Sealed Secrets, External Secrets, or Vault-managed values before deployment."
type: Opaque
stringData:
  DATABASE_URL: "postgres://htg:REPLACE_ME@postgres:5432/htgcrm?sslmode=disable"
  POSTGRES_PASSWORD: "REPLACE_ME"
  REDIS_PASSWORD: ""
  RABBITMQ_PASSWORD: "REPLACE_ME"
  RABBITMQ_URL: "amqp://htg:REPLACE_ME@rabbitmq:5672/"
  CLICKHOUSE_PASSWORD: "REPLACE_ME"
  KEYCLOAK_ADMIN_CLIENT_SECRET: "REPLACE_ME"
  KEYCLOAK_CLIENT_SECRET: ""
  NEXTAUTH_SECRET: "REPLACE_ME"
  OPENAI_API_KEY: "REPLACE_ME"
  SMTP_HOST: "REPLACE_ME"
  SMTP_PORT: "587"
  SMTP_USER: "REPLACE_ME"
  SMTP_PASS: "REPLACE_ME"
  SMTP_FROM: "REPLACE_ME"
  SMTP_ADMIN_EMAIL: "REPLACE_ME"
  PUSH_WEBHOOK_URL: ""
  HCS_ENDPOINT: "REPLACE_ME"
  HCS_USERNAME: "REPLACE_ME"
  HCS_PASSWORD: "REPLACE_ME"
  HCS_DOMAIN_ID: "REPLACE_ME"
